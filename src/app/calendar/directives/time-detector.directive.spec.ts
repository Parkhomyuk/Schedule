import { TimeDetectorDirective } from './time-detector.directive';

describe('TimeDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
